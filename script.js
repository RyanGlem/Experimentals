// Obj. literal var objName = {} properties

// Obj constuctor
function postion(x, y) {

    this.x = x;
    this.y = y;

    //Object method
    this.getPosition = function () {
        return (x,y);
    }

    this.changePosition = cPos;
}

function cPos (xpos, ypos){

    this.x = xpos;
    this.y = ypos;
}

document.write("wow");
var box = new position(10, 4);
document.body.innterHTML = box;


